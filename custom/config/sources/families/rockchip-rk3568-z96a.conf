#
# SPDX-License-Identifier: GPL-2.0
#
# Copyright (c) 2013-2023 Igor Pecovnik, igor@armbian.com
#
# This file is a part of the Armbian Build Framework
# https://github.com/armbian/build/
#
source "${BASH_SOURCE%/*}/include/rockchip64_common.inc"

# Use kemp233's u-boot fork with z96a defconfig (rockchip-v2026.01 base)
BOOTSOURCE='https://github.com/kemp233/u-boot-1.git'
BOOTBRANCH='branch:rockchip-v2026.01'
BOOTPATCHDIR="legacy"
OVERLAY_PREFIX='rk3568-z96a'

# Override default rockchip64 BOOTSCRIPT/BOOTENV_FILE (set by included
# rockchip64_common.inc) with the z96a-specific boot script that handles
# RK809 DCDC_REG4 (vdd_npu) -> 0.9V via I2C before kernel boot.
# Without this, the vdd_npu rail sits at 0.5V from reset and kernel NPU
# probe triggers "synchronous external abort" on iommu@fde4b000.
BOOTSCRIPT='boot-z96a-rk3568-laptop-v2.cmd:boot.cmd'

case $BRANCH in

	legacy)
		UBOOT_COMPILER="aarch64-linux-gnu-"
		UBOOT_USE_GCC='< 13'
		BOOTDIR='u-boot-rockchip64'
		KERNELSOURCE='https://github.com/kemp233/linux-rockchip.git'
		KERNELBRANCH='branch:rk-6.1-rkr5.1'
		declare -g -i KERNEL_GIT_CACHE_TTL=120
		declare -g KERNEL_MAJOR_MINOR="6.1"
		KERNELPATCHDIR='rockchip-rk3568-z96a/legacy'
		;;

	edge)
		UBOOT_COMPILER="aarch64-linux-gnu-"
		UBOOT_USE_GCC='< 13'
		BOOTDIR='u-boot-rockchip64'
		KERNELSOURCE='https://github.com/kemp233/linux-rockchip.git'
		KERNELBRANCH='branch:rk-6.6-rkr6.6'
		declare -g -i KERNEL_GIT_CACHE_TTL=120
		declare -g KERNEL_MAJOR_MINOR="6.6"
		KERNELPATCHDIR='rockchip-rk3568-z96a/legacy'
		;;

esac

prepare_boot_configuration

# Enable Armbian arm64 repository for rockchip multimedia packages (MPP/RGA/VA-API)
ARMBIAN_REPOSITORY_URL="https://apt.armbian.com"
ARMBIAN_REPOSITORY_KEY_URL="https://apt.armbian.com/armbian.gpg"
ARMBIAN_REPOSITORY_COMPONENTS="main rockchip-rk3568 utils"

# U-Boot target map override (kemp233/u-boot-1@rockchip-v2026.01 uses binman flash.bin)
function post_family_config__z96a_uboot_v2026_target_map() {
	display_alert "Z96A" "override UBOOT_TARGET_MAP for u-boot-1 binman" "info"
	BOOTDELAY=3
	DDR_BLOB="${DDR_BLOB:-"rk35/rk3568_ddr_1560MHz_v1.13.bin"}"
	BL31_BLOB="${BL31_BLOB:-"rk35/rk3568_bl31_v1.43.elf"}"
	UBOOT_TARGET_MAP="ROCKCHIP_TPL=${RKBIN_DIR}/${DDR_BLOB} BL31=${RKBIN_DIR}/${BL31_BLOB} spl/u-boot-spl u-boot.bin flash.bin;;idbloader.img u-boot.itb"
	display_alert "Z96A" "UBOOT_TARGET_MAP=${UBOOT_TARGET_MAP}" "info"
}

# Override default DDR blob with board-specific one and re-expand UBOOT_TARGET_MAP
function pre_config_uboot_target__z96a_install_ddr_and_fix_map() {
	local src1="${SRC}/packages/blobs/rk35/rk3568_ddr_1184MHz_android5042_z96a.bin"
	local src2="${SRC}/packages/blobs/rockchip/z96a/rk3568_ddr_1184MHz_android5042_z96a.bin"
	local dst_dir="${SRC}/cache/sources/rkbin-tools/rk35"
	local dst="${dst_dir}/rk3568_ddr_1184MHz_android5042_z96a.bin"
	if [[ -f "${src1}" || -f "${src2}" ]]; then
		local src="${src1}"
		[[ -f "${src1}" ]] || src="${src2}"
		mkdir -p "${dst_dir}"
		run_host_command_logged cp -pv "${src}" "${dst}"
		DDR_BLOB="rk35/rk3568_ddr_1184MHz_android5042_z96a.bin"
		display_alert "Z96A" "installed custom DDR ${DDR_BLOB}" "info"
	fi
	UBOOT_TARGET_MAP="ROCKCHIP_TPL=${RKBIN_DIR}/${DDR_BLOB} BL31=${RKBIN_DIR}/${BL31_BLOB} spl/u-boot-spl u-boot.bin flash.bin;;idbloader.img u-boot.itb"
	display_alert "Z96A" "make vars TPL=${RKBIN_DIR}/${DDR_BLOB} BL31=${RKBIN_DIR}/${BL31_BLOB}" "info"
	if [[ ! -f "${RKBIN_DIR}/${BL31_BLOB}" ]]; then
		display_alert "Z96A" "BL31 missing: ${RKBIN_DIR}/${BL31_BLOB}" "wrn"
		ls -la "${RKBIN_DIR}/rk35/" 2>/dev/null | head -50 || true
	fi
	if [[ ! -f "${RKBIN_DIR}/${DDR_BLOB}" ]]; then
		display_alert "Z96A" "DDR missing: ${RKBIN_DIR}/${DDR_BLOB}" "wrn"
		ls -la "${RKBIN_DIR}/rk35/" 2>/dev/null | head -50 || true
	fi
}

family_tweaks_bsp() {
	# USB RTL88x2BU needs the Debian Realtek firmware package.
	add_packages_to_image firmware-realtek

	# Rockchip Multimedia (MPP/RGA/VA-API) for hardware video decode
	add_packages_to_image rockchip-multimedia librockchip-mpp1 librga2 firmware-rockchip

	# RTL8822CS (SDIO) WiFi firmware for the vendored rkwifi driver
	local fwsrc="${SRC}/packages/blobs/wifi-rtl8822cs"
	if [[ -d "${fwsrc}" ]]; then
		mkdir -p "${destination}"/lib/firmware
		run_host_command_logged cp -v "${fwsrc}"/rtl8822cs_fw.bin "${destination}"/lib/firmware/
		run_host_command_logged cp -v "${fwsrc}"/rtl8822cs_config "${destination}"/lib/firmware/
	fi
}